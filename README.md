# actions-1
We will be learning github actions workflow
Adding this line to test [skip_ci] , need to put this command while commiting the code and the workflow will not run, rather it will be skipped.