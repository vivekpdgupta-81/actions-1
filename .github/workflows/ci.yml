name: My first workflow

on: push

jobs:
    fisrt_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "My First Github Actions job"

        - name: list files
          run: ls -lart

        - name: read files
          run: cat README.md